{{- if .Values.hpa.stargate.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: stargate
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: stargate
  minReplicas: {{ .Values.hpa.stargate.minReplicas }}
  maxReplicas: {{ .Values.hpa.stargate.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.hpa.stargate.targetCPU }}
{{- end }}
---
{{- if .Values.hpa.starlight.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: starlight-api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: starlight-api
  minReplicas: {{ .Values.hpa.starlight.minReplicas }}
  maxReplicas: {{ .Values.hpa.starlight.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.hpa.starlight.targetCPU }}
{{- end }}
